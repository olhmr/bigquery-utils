# Optional default arguments
# These can also be inferred from your gcloud auth setup
DEFAULT_BIGQUERY_CONFIG = {
    "project": None,
    "credentials": None,
    "location": "EU",
    "query_job_config": None,
}
